[project]
name = "f5-backup"
version = "0.2.0"
description = "Back up F5 BIG-IP configs"
requires-python = ">=3.12"
dependencies = ["requests>=2.32", "paramiko>=3.5"]

[dependency-groups]
dev = ["pytest>=8"]

[tool.pytest.ini_options]
pythonpath = ["."]
